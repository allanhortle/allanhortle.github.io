import MainLayout from '../../components/MainLayout';
import {graphql} from 'gatsby';
import {MDXRenderer} from 'gatsby-plugin-mdx';

# Spruce

<MDXRenderer>{props.data.readme.childMdx.body}</MDXRenderer>

export const pageQuery = graphql`
query {
    readme(path:{eq:"/blueflag/oose/master/packages/oose-documentation/docs/spruce/spruce-naming.md"}) {
        childMdx {body}
    }
}
`;

export default MainLayout;
